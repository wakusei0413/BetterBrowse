# BetterBrowse Milestone 3

展示版本：`Milestone 3`（2026-09-01）  
安装包版本号仍是 `1.0.0`，只改了扩展里显示的版本名。

这一版把收纳数据从「只存在本机」扩成「本机为主、可选同步到自己的网盘，也可以让本机 AI 助手代为操作」。链接跳转、P0～P3 智能收纳、阶梯式降级、OneTab 互导这些 Milestone 2 已有的能力还在，没有换掉。

---

## 这一版新增了什么

### 1. WebDAV 多设备同步

打开扩展选项页的 **云端同步**，填入自己的 WebDAV 地址（必须是 `https://`），保存凭据后即可在多台电脑之间同步收纳组、页面条目、规则和配置。

适用：Nextcloud、坚果云，以及常见网盘提供的 WebDAV。

同步时会做这些事：

- 本机改动会上传，其他设备的改动会拉下来合并
- 两台设备同时改了同一字段时，不会悄悄覆盖，会留在 **冲突裁决** 里让你选
- 连续 90 天没同步的设备会自动退役，也可以在 **设备管理** 里提前退役
- 远端数据校验失败时，可以用 **回退上一份快照** 或 **从本地快照重建**

也可以只同步偏好、不同步收纳列表：选项页里的 **随浏览器账号同步偏好** 会把阈值、规则开关、收纳箱设置和 WebDAV 地址写进 Chrome / Edge 账号；收纳列表和密码不走这条通道。

### 2. AI 桥接（默认关闭）

打开选项页的 **AI 桥接**，打开「允许本机 AI 助手操控插件」后，本机上的 AI 助手可以按规范读写收纳箱、改规则和配置。能力和你在界面上能做的事对齐。

安全边界：

- 默认关闭，不开就不会连
- 只走本机通道，不会把收纳箱暴露到公网
- WebDAV 密码对 AI 不可读
- 删除、清空这类操作必须带明确确认，不会因为你在界面里关了二次确认就被跳过

首次使用需要安装本机宿主。在选项页复制扩展 ID，然后执行：

```bash
deno task ai-host-install --ext-id=<扩展ID>
```

卸掉用 `deno task ai-host-uninstall`。

### 3. 本地数据更完整

Milestone 2 已经把收纳组放进 IndexedDB。这一版把配置、域名跳转规则、自动备份和访问活跃度也迁进同一套本地库。数据量大时读写更稳，升级后会自动迁移，不用手动搬家。

收纳箱里可以编辑条目标题、复制链接，组内检索和分页也更跟得上条目变多的情况。

### 4. 选项页多了两个入口

- **运行日志**：按级别、来源筛选扩展运行记录，方便排查同步或收纳为什么没按预期走
- **云端同步 / AI 桥接**：上面两套能力的设置都在这里，不用翻隐藏项

---

## 怎么开始用新功能

1. 在 `chrome://extensions/` 刷新已加载的 BetterBrowse，确认展示版本是 Milestone 3。
2. 打开扩展选项页。
3. 要用多设备同步：进 **云端同步** → 填 `https://` 地址和账号 → **保存凭据** → **测试连接** → 打开「启用云端同步」。建议先 **立即同步** 看状态是否正常。
4. 要用 AI 助手：先安装本机宿主，再在 **AI 桥接** 打开开关，用 **刷新状态** 确认已连接。

不配 WebDAV、不开 AI 桥接，本机收纳和跳转规则照常工作。

---

## 使用时要注意

- WebDAV 必须 HTTPS。密码只存在这台电脑，不会进同步文件，也不会进 JSON 导出。
- 同步的是收纳数据和配置，不是浏览器当前打开的标签窗口布局。
- 本地自动快照仍只留在本机，不会上传到 WebDAV。
- 当前版本不会对同步内容做端到端加密，网盘管理员能看到同步过去的网址和标题。只建议用自己控制的 WebDAV。
- AI 桥接只服务本机助手。没装宿主、或开关没开，状态会显示未启用 / 宿主缺失。

---

## 相对 Milestone 2，没变的部分

- 三态链接跳转（自动 / 当前标签 / 新标签）和按域名覆盖
- P0～P3 收纳规则，以及标签超标后的阶梯式降级
- 首位常驻收纳箱、右键菜单、OneTab 文本 / JSON 导入导出
- 浅色 / 深色外观

升级后如果选项页打不开、或收纳列表暂时是空的，先刷新扩展再看。数据在本地库里，不会因为刷新扩展被清掉。
